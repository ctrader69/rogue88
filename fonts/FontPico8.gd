extends Node2D

var font = null

func _ready():
	pass
	#print("FontPico8::ready()")
	#font = FontFile.new()
	#font.add_texture(load("res://fonts/pico8font.png"))
	#font.height = 6
	#var offset = 0
	#for c in range(32, 122):
		#font.add_char(c, 0, Rect2(offset, 0, 3, 5), Vector2(0, 0), 3.5)
		#offset += 4
